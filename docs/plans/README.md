# Historical Plans

This folder stores historical design plans.

Rules:

- Plans are context, not current source of truth.
- Current behavior lives in `docs/architecture/`.
- Process decisions live in `docs/decisions/`.
- If an old plan conflicts with `AGENTS.md` or `docs/README.md`, follow the
  current docs.
